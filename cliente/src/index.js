import React from 'react';
import ReactDOM from 'react-dom';

import App from './App'

ReactDOM.render(<App />, document.querySelector('#root'));    //en root (el unico div dentro de body de index.html) se inserta la App
